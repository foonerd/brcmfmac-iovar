# armhf - Raspberry Pi 2/3/4 (armv7l, 32-bit)
FROM arm32v7/debian:bookworm

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    build-essential \
    pkg-config \
    libnl-3-dev \
    libnl-genl-3-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /build
